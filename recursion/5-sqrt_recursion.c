#include "main.h"

/**
 * _sqrt_recursion - Natural square root of number
 * @n: size
 * Return: (int) number
 */
int _sqrt_recursion(int n)
{
	if (n <= 0)
	{
		return (-1);
	}
	return (x * _pow_recursion(x, y - 1));
}
